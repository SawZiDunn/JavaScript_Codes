const zidunn = ["saw", "father", "mother", "son"]

zidunn.push("How are you?");//put data from behind
zidunn.unshift("Identity");//put data from the front

zidunn.pop();  //remove data from behind
zidunn.shift();   //remove data from the front

console.log(zidunn);